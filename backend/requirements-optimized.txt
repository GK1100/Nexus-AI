# -----------------------------
# Core Backend (Minimal)
# -----------------------------
fastapi==0.115.12
uvicorn[standard]==0.34.2  # Include standard for better performance
python-multipart==0.0.20
pydantic==2.11.3
python-dotenv==1.2.1

# -----------------------------
# Embeddings & Transformers (Optimized versions)
# -----------------------------
sentence-transformers==2.6.1
transformers==4.41.2
torch==2.2.2  # CPU-only for Render free tier
accelerate==0.30.1

# -----------------------------
# LangChain (Minimal - only what you need)
# -----------------------------
langchain-core==1.1.0
langchain-community==0.4.1
langchain-huggingface==1.2.0
langchain-groq==1.1.0
langchain-text-splitters==1.0.0

# -----------------------------
# Vision & Image Processing
# -----------------------------
pillow==12.0.0
easyocr==1.7.2
opencv-python-headless==4.9.0.80  # Headless version is lighter

# -----------------------------
# Vector Database (Choose ONE)
# -----------------------------
pinecone==7.3.0
# chromadb==1.3.5  # Comment out if using Pinecone only

# -----------------------------
# LLM Provider
# -----------------------------
groq==0.30.0

# -----------------------------
# File Handling (Minimal)
# -----------------------------
python-docx==1.2.0
pypdf==6.5.0

# -----------------------------
# Utilities
# -----------------------------
numpy==1.26.4

# -----------------------------
# Async / Performance
# -----------------------------
aiofiles==24.1.0

# -----------------------------
# Optional: Evaluation (Remove for production)
# -----------------------------
# ragas 
# datasets 
# openai==2.15.0
# tiktoken
